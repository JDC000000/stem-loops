# S1 — Decision Record: YouTube Download Path Bake-off

- **Spike:** TSD §8 / S1 · Component C4 · de-risks R1 (the #1 risk)
- **Success criterion (Gate 0):** chosen path ≥95% success across 50 representative URLs; p90 fetch <10s. Prefer lower operational burden when both pass.
- **Date run:** 2026-06-30 · **Status:** architecture recommended; **headline metric NOT measurable in this environment** (see Blockers).

## What was exercised
Two download paths were stood up as the smallest possible experiment — no UI, no queue:
- **Cobalt** (self-hosted, Fly): `spikes/s1_bakeoff/cobalt_client.py` + deploy spec `cobalt/Dockerfile` + `cobalt/fly.toml` (scale-to-zero). Sidesteps cookies entirely (satisfies anti-goal #1).
- **yt-dlp + server-managed cookie rotation:** `spikes/s1_bakeoff/ytdlp_client.py`. Cookie pool is server-side only; cookie/token material redacted from captured stderr.
- **Harness:** `spikes/s1_bakeoff/harness.py` — runs N URLs through both concurrently, reports per-path success rate + p90 latency to `s1_results.csv`.

## Measured this session (smoke test, 1 URL)
| Client | Result | Interpretation |
|---|---|---|
| `cobalt_client` | `{success:false, latency_ms:250, error:"Name or service not known"}` | Clean dict, no exception. Failure = Cobalt instance **not deployed** (DNS). |
| `ytdlp_client` | `{success:false, latency_ms:1, error:"yt-dlp binary not installed"}` | Clean dict, no exception. `yt-dlp` CLI not on PATH; `ffmpeg` also absent. |

**Robustness confirmed:** both clients return a measured dict and never raise — the harness can score a full run unattended.

## Blockers — why the ≥95% / p90 metric cannot be measured here (honest)
1. **Cobalt not deployed** — no `fly`/`flyctl` CLI or Fly.io account in this sandbox.
2. **No `yt-dlp` binary + no `ffmpeg`** on PATH — `-x --audio-format wav` cannot transcode.
3. **Only 1 of 50 URLs provided** — `fixtures/fixture_urls.txt` has the Gate 2 demo URL only; the 50-URL set is operator-provided.
4. **Datacenter IP** — sandbox egress is bot-challenged by YouTube, so any number measured here would *understate* the real (Fly/residential) rate. This is the exact V1 failure mode the spike exists to neutralise.

## Results table — bake-off RUN 2026-06-30 (real, 50 live URLs)
50-URL set built via `yt-dlp ytsearch` (genre-balanced: hip-hop 90s+modern / lofi / neo-soul / drill / electronic; mixed era/popularity/duration) → `spikes/fixtures/fixture_urls.txt`. Runner: `spikes/s1_bakeoff/datacenter_bakeoff.py`.

| Path | Success/50 | p50/p90 (success) | Notes |
|---|---|---|---|
| **yt-dlp** (datacenter IP, no cookies) | **0/50 (0%)** | n/a | **100% BOT_BLOCK** ("Sign in to confirm you're not a bot"). Re-tested across player_client = tv/ios/mweb/web_safari/android/web_embedded/tv_embedded — **all blocked**. |
| **Cobalt v10.9.4** (self-hosted, datacenter IP, no cookies) | **0/50 (0%)** | n/a | **100% `error.api.youtube.login`** (serial, rate-limit raised — clean). Same YouTube login/bot wall. Run on local Docker (`ghcr.io/imputnet/cobalt:10`); the supplied Fly token was app-scoped and could not create the spike app, but local Docker is the **same datacenter-IP class** as Fly and the determinant is datacenter-vs-residential, not the specific host. |

**GATE 0 (S1) verdict: FAIL for both paths** — 0% ≪ 95% required; p90 n/a (zero successes).

**R1 implication (decisive):** from a datacenter IP **without YouTube auth, NEITHER path can fetch** — self-hosting Cobalt does **not** bypass the bot-gate (it hits the identical login wall). The PRD bans residential proxies, so this is the production-like result. **The only remaining viable lever is a server-managed YouTube cookie / PO-token pool** (feeding yt-dlp `--cookies`, or Cobalt's `cookies.json` / youtube-session-token server). That reintroduces the cookie-pool operational burden the PRD hoped to avoid — but the data shows it is unavoidable for datacenter-only egress. **This is an architecture decision for the operator before any T12 wiring.**

## Developer recommendation (pre-measurement)
Lean **Cobalt primary + yt-dlp fallback.** Cobalt removes cookie-pool maintenance (anti-goal #1) and the dual-path design keeps yt-dlp as a resilient fallback. Lower operational burden wins the tie-break if both clear the bar.

## ⛔ Hard pre-T12 production gate (still OPEN)
The ≥95% / p90<10s bake-off **MUST run and pass before T12** (real YouTube download wiring). It is independent of approving the architecture now (Phase 1 uses a **stub** downloader and makes no real YouTube call). Operator infra needed, flag with lead time:
1. Fly.io account + CLI → `fly deploy --config cobalt/fly.toml`.
2. `ffmpeg` + `yt-dlp` binary in the execution environment.
3. `apps/worker/spikes/fixtures/fixture_urls.txt` populated with 50 representative URLs.
4. Run `python harness.py ../fixtures/fixture_urls.txt`, fill the table + pick the winner.

---
**DECISION NEEDED (bake-off has now RUN — gate FAILED):** Both no-auth datacenter paths score **0% (yt-dlp 0/50 bot-block; Cobalt 0/50 youtube.login)** — the ≥95%/<10s gate **fails**, and self-hosting Cobalt does **not** fix it. The operator must now choose the YouTube-fetch strategy before T12: **(1) adopt a server-managed cookie / PO-token pool** (yt-dlp `--cookies` or Cobalt `cookies.json` / youtube-session-token server) — the only datacenter-compatible path, at the cost of cookie-pool maintenance the PRD hoped to avoid; **(2) revisit the residential-proxy ban** (PRD currently prohibits); or **(3) re-scope** YouTube ingestion. Recommendation: pursue (1) — a small server-side authenticated YouTube session/cookie pool feeding Cobalt-primary + yt-dlp-fallback — then re-run this bake-off to clear ≥95% before T12. **T12 stays UNWIRED until a path clears the gate AND the operator ratifies.**

# Node
node_modules/
.next/
dist/
.env
.env.local
.env.*.local
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Build artifacts
apps/worker/spikes/s1_bakeoff/s1_results.csv

# Spike fixtures — regenerable synthetic audio (run make_synthetic_fixtures.py).
# The generator + ground-truth JSON + measured results JSON ARE committed; the
# WAVs are not (binary, deterministic, ~6MB).
apps/worker/spikes/fixtures/*.wav
apps/worker/spikes/fixtures/*.mp3
apps/worker/spikes/fixtures/*.flac

# Secrets
*.pem
*.key
apps/worker/tests/fixtures/*.wav
